#include<stdio.h>
int loop(int i, int x){
  if(i<=x){
    printf("%d",x);
  if(i<x){
    printf(" ");
  }
    loop(i,x-1);
  }
  return 0;
}


int main(){
 int x;
 scanf("%d",&x);
 loop(1,x);
}
