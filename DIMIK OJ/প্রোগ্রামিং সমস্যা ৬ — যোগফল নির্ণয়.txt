#include <stdio.h>
int main() {
    int t, n, f, l;
    scanf("%d", &t);
    for (int i = 0; i < t; i++) {
        scanf("%d", &n);
         f = n / 10000;
         l  = n % 10;
        printf("Sum = %d\n",f+l);
    }
}