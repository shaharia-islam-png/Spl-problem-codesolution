#include <stdio.h>
int main()
{
   float N;
    int x;
    scanf("%f", &N);
    x = (int)N; 
    if(N == x){
      printf("int %d\n", x);
     }
    else{
      printf("float %d %.3f\n", x, N - x);
     }
}