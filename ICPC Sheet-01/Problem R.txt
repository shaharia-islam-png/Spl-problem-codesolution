#include <stdio.h>
int main() 
{
    int N,m,d,y;
    scanf("%d", &N);
    y = N / 365;
    N = N % 365;
    m = N / 30;
    d = N % 30;
    printf("%d years\n", y);
    printf("%d months\n", m);
    printf("%d days\n", d);
}