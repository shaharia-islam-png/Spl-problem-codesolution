#include <stdio.h>
int main() {
    char name[51];       
    double salary, sales;
    scanf("%s", name);
    scanf("%lf", &salary);
    scanf("%lf", &sales);
    double total = salary + (sales * 0.15);
    printf("TOTAL = R$ %.2lf\n", total);

    return 0;
}
